import { HttpClient } from '../client/types';
import { SocialUploadOptions, UploadResult } from '../types';
/**
 * Send a social media post to the API for analysis
 * @param client HTTP client
 * @param options Upload options
 * @returns Upload result with request and media IDs
 */
export declare function uploadSocialMediaLink(client: HttpClient, options: SocialUploadOptions): Promise<UploadResult>;
